{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/11-Largest product in a grid.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}